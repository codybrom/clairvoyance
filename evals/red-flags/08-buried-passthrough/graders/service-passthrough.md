---
type: llm
focus: last_message
---
PASS if the answer identifies `UserService.find_by_email` as a pass-through that only forwards to `store.find_by_email` and adds nothing, distinguishing it from the service's real methods. FAIL if not mentioned.
